# 智能社区服务平台测试报告

## 1. 测试目的

验证本次二次质检整改后，系统在构建层面与核心功能层面能够满足课程验收要求，并形成可提交的测试说明材料。

## 2. 测试时间

- 测试日期：2026-03-16

## 3. 测试环境

| 项目 | 环境 |
| --- | --- |
| 操作系统 | macOS 开发环境 |
| 前端 | Node.js + Vite |
| 后端 | Docker 构建 Java/Tomcat 镜像 |
| 数据库 | MySQL 8.0（Docker Compose） |
| 容器工具 | Docker 29.2.0 |

## 4. 测试范围

- 个人中心历史管理列表补齐
- 我的报修更新时间展示优化
- 活动签到二维码展示优化
- 活动回顾照片布局优化
- 物业服务中心访客登记按钮布局优化
- 访客登记重复提示收敛
- 文档交付完整性检查

## 5. 已执行验证

### 5.1 前端依赖安装

执行命令：

```bash
cd frontend
npm install
```

结果：

- 执行成功。
- npm 输出提示存在 2 个 moderate 级依赖告警，未影响本次构建通过。

### 5.2 前端生产构建

执行命令：

```bash
cd frontend
npm run build
```

结果：

- 构建成功。
- Vite 完成全部模块转换并输出 `dist/` 产物。
- 存在大体积 chunk 警告，但不影响构建通过与功能运行。

### 5.3 Compose 配置校验

执行命令：

```bash
docker compose config
```

结果：

- 校验通过。
- 前端、后端、数据库三服务配置展开正常。

### 5.4 后端镜像构建

执行命令：

```bash
docker compose build backend
```

结果：

- 构建成功。
- Maven 依赖与 WAR 打包链路在 Dockerfile 中可正常完成。

### 5.5 本机 Maven 验证说明

执行命令：

```bash
cd backend
mvn -q -DskipTests package
```

结果：

- 当前环境未安装 `mvn`，本机命令无法直接执行。
- 已改用项目实际交付链路 `docker compose build backend` 完成等效构建验证。

## 6. 功能整改项检查清单

| 编号 | 检查项 | 整改结果 |
| --- | --- | --- |
| 1 | 二维码下方多余文字 | 已删除原始签到 payload 文本，改为提示文案 |
| 2 | 个人中心缺少我的报修/我的活动详尽列表 | 已在个人空间中新增历史管理列表与分页 |
| 3 | 活动回顾照片布局较弱 | 已改为图文分栏卡片式布局 |
| 4 | 我的报修更新时间显示不佳 | 已改为“绝对时间 + 相对时间”双层展示 |
| 5 | 物业服务中心登记访客按钮布局 | 已调整为独立操作栏并适配移动端 |
| 6 | 登记访客出现两次 toast 提示 | 已增加实时通知去重处理，避免本地成功提示与站内推送重复弹出 |
| 7 | 核心交付文档缺失 | 已补齐 6 份正式文档 |

## 7. 需说明的限制

- 本次在当前终端环境中完成了代码级检查、前端构建验证、Compose 配置验证与后端 Docker 构建验证。
- 未在图形化浏览器中逐项手工点击回归，因此 UI 最终视觉表现建议在答辩前再进行一次人工演示确认。

## 8. 测试结论

本次二次质检涉及的主要代码问题与交付物缺失问题已完成整改，前端构建通过、后端 Docker 构建通过、Compose 配置有效，项目已具备再次提交验收的基础条件。
